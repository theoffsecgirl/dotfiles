# NOTA: este script fue usado puntualmente para migrar aliases legacy
# al nuevo formato modular. Ya no tiene uso activo.
# Se conserva en tools/ como referencia histórica.
# Puedes borrarlo si no lo necesitas.

# (contenido original preservado abajo)
import os, re, sys

def main():
    print("Script de migración de aliases (uso puntual, ya ejecutado).")
    print("Ver vendor/shell-utils/zsh/ para la versión actual.")

if __name__ == "__main__":
    main()
