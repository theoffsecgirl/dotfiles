name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint shell (bash -n / zsh -n)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar zsh
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y zsh

      - name: Syntax check — configs zsh
        run: |
          fail=0
          for f in config/.config/zsh/*.zsh; do
            [ -f "$f" ] || continue
            echo "zsh -n $f"
            zsh -n "$f" || fail=1
          done
          exit "$fail"

  test:
    name: bats test/
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # zsh es necesario porque test_zsh.bats corre `zsh -n` sobre los configs
      - name: Instalar zsh
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y zsh

      # Mismo método que documenta test/README.md para Linux
      - name: Instalar bats-core
        run: |
          git clone https://github.com/bats-core/bats-core.git /tmp/bats-core
          sudo /tmp/bats-core/install.sh /usr/local

      # test/ son smoke tests — ver test/README.md.
      - name: Run bats
        run: bats --formatter tap test/
