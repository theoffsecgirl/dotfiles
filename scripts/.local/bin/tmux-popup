#!/bin/bash
# Tmux popup helper - terminal flotante rápido

if [[ $# -eq 0 ]]; then
    # Popup en directorio actual
    tmux display-popup -E -w 80% -h 80%
else
    # Ejecutar comando en popup
    tmux display-popup -E -w 80% -h 80% "$@"
fi
