#!/usr/bin/env bash
# DEPRECADO — este script ya no se mantiene.
# Usa: claude-recon-v2
set -euo pipefail

printf '\033[1;33m[!]\033[0m %s\n' "DEPRECADO: claude-recon → usa claude-recon-v2" >&2
exec "$(dirname "${BASH_SOURCE[0]}")/claude-recon-v2" "$@"
