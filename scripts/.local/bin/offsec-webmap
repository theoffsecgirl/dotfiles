#!/usr/bin/env bash
# RETIRADO 2026-08-21 — cero uso real confirmado por auditoría, ver historial de git para recuperar
# set -euo pipefail

# target="${1:-}"
# [[ -n "$target" ]] || { echo "Uso: offsec webmap <programa>" >&2; exit 1; }

# webmap "$target"
