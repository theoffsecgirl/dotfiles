#!/usr/bin/env bash
# scope — wrapper estable hacia scope-v2
# Uso: scope <dominio>
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/scope-v2" "$@"
